# thatssoheil

Personal website. Next.js (App Router) + Tailwind v4 + shadcn + GSAP, deployed to Cloudflare via OpenNext.

## Agent skills

### Issue tracker

GitHub Issues for `thatssoheil/thatssoheil` via the `gh` CLI; external PRs are not a triage surface. See `docs/agents/issue-tracker.md`.

### Triage labels

Canonical five-role vocabulary, label strings unchanged. See `docs/agents/triage-labels.md`.

### Domain docs

Single-context: `CONTEXT.md` + `docs/adr/` at the repo root. See `docs/agents/domain.md`.
